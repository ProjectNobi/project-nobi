# Project Nobi — Dependencies
# Bittensor subnet for personal AI companions

bittensor>=10.2.0
openai>=1.0.0
requests>=2.31.0
numpy>=1.24.0
pydantic>=2.0.0
python-telegram-bot>=21.0
cryptography>=41.0.0

# Semantic memory (optional — falls back to TF-IDF if not installed)
# sentence-transformers uses ~80MB model (all-MiniLM-L6-v2), no GPU required
sentence-transformers>=2.2.0
scikit-learn>=1.3.0
scikit-learn>=1.3.0

# Optional: High-quality semantic embeddings (recommended for validators)
# Install with: pip install sentence-transformers
# Without this, the system falls back to TF-IDF (still works, lower quality)
# sentence-transformers>=2.2.0
